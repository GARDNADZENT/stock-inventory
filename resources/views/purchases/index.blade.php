@extends('layouts.app')
@section('content')<div class="d-flex justify-content-between mb-3"><h1 class="h3">Purchases</h1><a href="{{ route('purchases.create') }}" class="btn btn-success">New Purchase</a></div><div class="bg-white rounded p-3 table-responsive"><table class="table"><thead><tr><th>Date</th><th>Supplier</th><th class="text-end">Total</th><th></th></tr></thead><tbody>@foreach($purchases as $purchase)<tr><td>{{ $purchase->purchase_date->format('Y-m-d') }}</td><td>{{ $purchase->supplier?->supplier_name ?? 'Walk-in' }}</td><td class="text-end">{{ number_format($purchase->total_amount, 2) }}</td><td class="text-end"><a href="{{ route('purchases.show', $purchase) }}" class="btn btn-sm btn-outline-primary">View</a></td></tr>@endforeach</tbody></table>{{ $purchases->links() }}</div>@endsection
