@extends('layouts.app')
@section('content')<div class="d-flex justify-content-between mb-3"><h1 class="h3">Stock Takes</h1><a href="{{ route('stock-takes.create') }}" class="btn btn-success">New Stock Take</a></div><div class="bg-white rounded p-3 table-responsive"><table class="table"><thead><tr><th>Date</th><th>Status</th><th>Posted</th><th></th></tr></thead><tbody>@foreach($stockTakes as $stockTake)<tr><td>{{ $stockTake->stock_take_date->format('Y-m-d') }}</td><td>{{ ucfirst($stockTake->status) }}</td><td>{{ $stockTake->posted_at?->format('Y-m-d H:i') }}</td><td class="text-end"><a href="{{ route('stock-takes.show', $stockTake) }}" class="btn btn-sm btn-outline-primary">View</a></td></tr>@endforeach</tbody></table>{{ $stockTakes->links() }}</div>@endsection
