@extends('layouts.app')
@section('content')<div class="d-flex justify-content-between mb-3"><h1 class="h3">Stock Take #{{ $stockTake->id }}</h1>@if($stockTake->status !== 'posted')<form method="post" action="{{ route('stock-takes.post', $stockTake) }}">@csrf<button class="btn btn-warning">Post Adjustments</button></form>@endif</div><div class="bg-white rounded p-3 table-responsive"><table class="table"><thead><tr><th>Product</th><th class="text-end">System</th><th class="text-end">Counted</th><th class="text-end">Variance</th></tr></thead><tbody>@foreach($stockTake->items as $item)<tr><td>{{ $item->product->product_name }}</td><td class="text-end">{{ $item->system_quantity }}</td><td class="text-end">{{ $item->counted_quantity }}</td><td class="text-end">{{ $item->variance }}</td></tr>@endforeach</tbody></table></div>@endsection
